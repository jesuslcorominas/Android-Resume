<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jesuslcorominas.resume.app">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".view.activity.SplashActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.activity.MainActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.ExperiencesActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.OtherDataActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.KnowledgesActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.TrainingsActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.ExperienceDetailActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

        <activity
            android:name=".view.activity.ProjectDetailActivity_"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|keyboardHidden" />

    </application>

</manifest>
